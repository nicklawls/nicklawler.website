import { Options } from "$fresh/plugins/twind.ts";

export default {
  selfURL: import.meta.url,
  theme: {
    colors: { yellow: "#EFD28D", indigodye: "#004777", bittersweet: "#FF5A5F" },
  },
} as Options;
